
const linearSearch = (array, target) => {
  /*
   * Loop through the array, from start to finish for target
   * and return that element when it is found, or false if it is not
   */
};

module.exports = linearSearch;